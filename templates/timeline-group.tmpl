<div class="timeline-group" onclick="$(window).trigger('selectTrack', ['{{id}}'])">
    <div class="track-id">{{id}}</div>
    {{#if isMine}}<i onclick="$(window).trigger('deleteTrack', ['{{id}}'])" class="delete icon-trash" title="Delete  track"></i>
    {{#if isSupervisor}}<i onclick="$(window).trigger('updateTrack', ['{{id}}'])" class="visibility icon-eye-{{#if isPublic}}open{{else}}close{{/if}}" title="Change track visibility"></i>{{/if}}
    <i class="private icon-user" title="You own this track"></i>{{/if}}
    <a id="track{{id}}" onclick="$(window).trigger('selectTrack', ['{{id}}'])"><div class="content" title="{{name}}" rel="popover" data-content="{{#if description}}{{description}}{{else}}No description{{/if}}" data-original-title="{{name}}">{{name}}</div></a>
</div>

