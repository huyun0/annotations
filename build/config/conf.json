{
    "plugins": [],
    "templates": {
        "cleverLinks": true,
        "monospaceLinks": true
    },
    "opts": {
        "template": "DataTables",         // same as -t default
        "destination": "./testOut/",
        "encoding": "utf8",               // same as -e utf8
        "recurse": true,                  // same as -r
    }
}